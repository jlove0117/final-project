<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cst2335.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".FoodNutritionDatabase" />
        <activity android:name=".CBCNewsReader" />
        <activity android:name=".MovieInformation" />
        <activity android:name=".OCTranspoBusRouteApp" />
        <activity android:name=".StartProjectActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OCTrip" />
        <activity android:name=".OCTranspoBusRouteDetails" />
        <activity android:name=".MovieDetailsFragment"></activity>
    </application>

</manifest>